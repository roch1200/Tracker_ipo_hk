name: hkex_ipo_tracker
description: "HKEX IPO Tracker - Seguiment d'IPOs del mercat de Hong Kong"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.2.0

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP & Networking
  http: ^1.2.0
  http_parser: ^4.0.0
  
  # Persistència local
  shared_preferences: ^2.5.3
  
  # Dates i formateig
  intl: ^0.19.0
  
  # UI
  cupertino_icons: ^1.0.6
  flutter_spinkit: ^5.2.0
  fl_chart: ^0.68.0
  
  # State management
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

